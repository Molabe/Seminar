package kontrolaRetazca;

public class Helper {
    public void vypocetPismen(String retazec) {
        int pocetPismen = 0;

        pocetPismen = retazec.length();
        System.out.println(pocetPismen);
    }
}
